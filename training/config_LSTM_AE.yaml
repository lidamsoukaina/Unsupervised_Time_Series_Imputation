# learning_rate
learning_rate: 0.01
# reduction parameter
reduction_parameter: 0.8
# train epochs
epochs: 50
# Sequence Length
sequence_length: 12
# Batch size
batch_size: 16
# ORT_MIT_LOSS parameter
loss_parameter: 0.5
# checkpoints paths
models_path: "./trained_models/LSTM_AE"
